org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError -XX:HeapBaseMinAddress=0x100000000
android.useAndroidX=true
# Disable Kotlin incremental compilation to avoid cross-drive path errors on Windows
# (pub cache on C:\ cannot be relativized to project on D:\)
kotlin.incremental=false
kotlin.incremental.android=false
`
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
